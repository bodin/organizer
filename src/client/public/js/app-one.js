/*import React from 'react';
import ReactDOM from 'react-dom';
import MuiThemeProvider from 'material-ui/styles/MuiThemeProvider';
import DocumentCard from './components/DocumentCard';
import { Grid, Row, Cell } from 'react-inline-grid-15';

import injectTapEventPlugin from 'react-tap-event-plugin';
injectTapEventPlugin();

var data = {
		title:'Tech List',
		subtitle:"Things I really need to read",
		dateCreated: new Date(),
		dateModified: new Date(),
		content:"http://asciidoctor.org[*Asciidoctor*] running on http://opalrb.org[_Opal_] brings AsciiDoc to Node.js!"
			
}

const App = () => (
	<MuiThemeProvider>
		<Grid>
			<Row is="center">
				<Cell is="3 tablet-4 phone-4"><DocumentCard {...data}/></Cell>
				
			</Row>
		</Grid>
	</MuiThemeProvider>
);

ReactDOM.render(
  <App />,
  document.getElementById('react-one')
);*/